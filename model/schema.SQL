CREATE DATABASE IF NOT EXISTS CODENAMES;

USE CODENAMES;

CREATE TABLE IF NOT EXISTS Boards (
  id INT AUTO_INCREMENT PRIMARY KEY,
  p1Agents INT NOT NULL,
  p1Sass INT NOT NULL,
  p2Agents INT NOT NULL,
  p2Sass INT NOT NULL
);
